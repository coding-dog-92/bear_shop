<bxr-carousel class="rw-carousel" role="region" aria-roledescription="carousel" aria-label="{{ section.settings.label | escape }}" data-immersive="{{ section.settings.immersive }}" data-autoplay="{{ section.settings.autoplay }}" data-interval="{{ section.settings.interval | times: 1000 }}">
  <div class="rw-slides">
    {% for block in section.blocks %}
      {% liquid
        assign artwork = block.settings.artwork | default: 'hero'
        assign fallback = 'rw-' | append: artwork | append: '.webp'
        if artwork == 'hero'
          assign fallback = 'bxr-clean-hero.webp'
        elsif artwork == 'slide-tires'
          assign fallback = 'bxr-clean-tires.webp'
        endif
        assign bundled_film = false
        assign has_video = false
        if block.settings.included_film and block.settings.video == blank
          assign bundled_film = true
          assign fallback = 'bxr-porsche-detail-film.webp'
        endif
        if block.settings.video != blank or bundled_film
          assign has_video = true
        endif
        assign mobile_fallback = fallback | replace: '.webp', '-mobile.webp'
        assign loading = 'lazy'
        assign priority = 'auto'
        if forloop.first
          assign loading = 'eager'
          assign priority = 'high'
        endif
      %}
      {% capture primary_destination %}{% if block.settings.primary_link != blank %}{{ block.settings.primary_link }}{% elsif block.settings.category != blank and block.settings.category != 'all' %}{% render 'bxr-category-url', category: block.settings.category, settings: settings %}{% else %}{{ routes.all_products_collection_url }}{% endif %}{% endcapture %}
      <section class="rw-hero rw-slide" id="BXRSlide-{{ section.id }}-{{ block.id }}" role="group" aria-roledescription="slide" aria-label="{{ forloop.index }} of {{ section.blocks.size }}" data-slide data-artwork="{{ artwork }}" data-content-position="{{ block.settings.content_position | default: 'left' }}" data-block-id="{{ block.id }}" data-photo-motion="{{ block.settings.photo_motion }}"{% if has_video %} data-has-video data-video-tone="{{ block.settings.video_tone }}"{% endif %} {% unless forloop.first %}hidden inert{% endunless %} {{ block.shopify_attributes }}>
        <picture class="rw-hero-media">
          {% if block.settings.mobile_image != blank %}
            <source media="(max-width: 749px)" srcset="{{ block.settings.mobile_image | image_url: width: 900 }}">
          {% elsif block.settings.image == blank %}
            <source media="(max-width: 749px)" srcset="{{ mobile_fallback | asset_url }}">
          {% endif %}
          {% render 'rw-image', image: block.settings.image, fallback: fallback, alt: block.settings.image_alt, loading: loading, priority: priority, sizes: '100vw', width: 1920, height: 1040 %}
        </picture>
        {% if block.settings.video != blank %}
          <div class="bxr-hero-film" aria-hidden="true" data-video-layout="{% if block.settings.mobile_video != blank %}desktop{% else %}all{% endif %}">
            {{ block.settings.video | video_tag: image_size: '1920x', autoplay: false, loop: true, muted: true, controls: false, preload: 'none', tabindex: '-1' }}
          </div>
          {% if block.settings.mobile_video != blank %}<div class="bxr-hero-film" aria-hidden="true" data-video-layout="mobile">{{ block.settings.mobile_video | video_tag: image_size: '900x', autoplay: false, loop: true, muted: true, controls: false, preload: 'none', tabindex: '-1' }}</div>{% endif %}
        {% elsif bundled_film %}
          {% render 'bxr-bundled-film' %}
        {% endif %}
        <div class="rw-hero-content">
          {% if forloop.first %}<h1 class="rw-hero-title">{% else %}<h2 class="rw-hero-title">{% endif %}<span>{{ block.settings.line_1 | escape }}</span><span>{{ block.settings.line_2 | escape }}</span>{% if block.settings.line_3 != blank %}<span>{{ block.settings.line_3 | escape }}</span>{% endif %}{% if forloop.first %}</h1>{% else %}</h2>{% endif %}
          <p>{{ block.settings.description | escape }}</p>
          <div class="rw-actions">
            <a class="rw-button" href="{{ primary_destination | strip }}">{{ block.settings.primary_label | escape }}{% render 'rw-icon', name: 'arrow' %}</a>
            {% if block.settings.secondary_label != blank %}<a class="rw-text-link" href="{{ block.settings.secondary_link | default: '#bxr-fitment' }}">{{ block.settings.secondary_label | escape }}{% render 'rw-icon', name: 'arrow' %}</a>{% endif %}
          </div>
        </div>
      </section>
    {% endfor %}
  </div>
  {% if section.blocks.size > 1 or section.settings.autoplay %}
    <div class="rw-carousel-controls" hidden>
      <div class="rw-carousel-dots" aria-label="Choose a slide">{% for block in section.blocks %}<button type="button" data-slide-to="{{ forloop.index0 }}" aria-label="Slide {{ forloop.index }}: {{ block.settings.line_1 | escape }} {{ block.settings.line_2 | escape }} {{ block.settings.line_3 | escape }}" aria-controls="BXRSlide-{{ section.id }}-{{ block.id }}" {% if forloop.first %}aria-current="true"{% endif %}><span class="rw-pagination-dot" aria-hidden="true"></span></button>{% endfor %}</div>
      {% if section.settings.autoplay %}<button type="button" data-playback aria-label="Pause slideshow"><span data-pause-icon>{% render 'rw-icon', name: 'pause' %}</span><span data-play-icon hidden>{% render 'rw-icon', name: 'play' %}</span></button>{% endif %}
    </div>
    <p class="visually-hidden" data-slide-status aria-live="polite" aria-atomic="true"></p>
  {% endif %}
</bxr-carousel>
{% schema %}
{
  "name": "BXR cinematic slideshow",
  "max_blocks": 5,
  "settings": [
    {
      "type": "checkbox",
      "id": "immersive",
      "label": "Full-screen cinematic layout",
      "default": false,
      "info": "Overlays navigation on the film, with an opaque header after scrolling. Use footage with clear space on the left for the headline."
    },
    {
      "type": "text",
      "id": "label",
      "label": "Accessible label",
      "default": "BXR highlights"
    },
    {
      "type": "checkbox",
      "id": "autoplay",
      "label": "Auto-rotate slides",
      "default": true
    },
    {
      "type": "range",
      "id": "interval",
      "label": "Seconds between slides",
      "min": 5,
      "max": 12,
      "step": 1,
      "unit": "s",
      "default": 7
    }
  ],
  "blocks": [
    {
      "type": "slide",
      "name": "Slide",
      "settings": [
        {
          "type": "select",
          "id": "content_position",
          "label": "Desktop text position",
          "options": [
            { "value": "left", "label": "Left" },
            { "value": "right", "label": "Right" }
          ],
          "default": "left"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Desktop image"
        },
        {
          "type": "image_picker",
          "id": "mobile_image",
          "label": "Mobile image"
        },
        {
          "type": "checkbox",
          "id": "included_film",
          "label": "Use included racing film",
          "default": false,
          "info": "A muted Porsche 911 test film, with a separate mobile crop. Selecting your own background video overrides this film."
        },
        {
          "type": "video",
          "id": "video",
          "label": "Background video"
        },
        {
          "type": "video",
          "id": "mobile_video",
          "label": "Mobile background video (optional)"
        },
        {
          "type": "paragraph",
          "content": "Upload a muted 8\u201312 second film through the video picker. Images remain the fallback when motion is disabled or video cannot play. Mobile uses the desktop video unless a portrait video is selected."
        },
        {
          "type": "checkbox",
          "id": "photo_motion",
          "label": "Slow camera movement on photos",
          "default": false
        },
        {
          "type": "select",
          "id": "video_tone",
          "label": "Text over video",
          "options": [
            {
              "value": "light",
              "label": "White text / dark film"
            },
            {
              "value": "dark",
              "label": "Dark text / light film"
            }
          ],
          "default": "light"
        },
        {
          "type": "text",
          "id": "image_alt",
          "label": "Image description",
          "default": "Silver track car in a sunlit pit lane"
        },
        {
          "type": "text",
          "id": "line_1",
          "label": "Headline line 1",
          "default": "For the"
        },
        {
          "type": "text",
          "id": "line_2",
          "label": "Headline line 2",
          "default": "next lap."
        },
        {
          "type": "text",
          "id": "line_3",
          "label": "Headline line 3"
        },
        {
          "type": "text",
          "id": "description",
          "label": "Supporting text",
          "default": "Racing tires and braking components."
        },
        {
          "type": "text",
          "id": "primary_label",
          "label": "Primary button",
          "default": "Explore parts"
        },
        {
          "type": "url",
          "id": "primary_link",
          "label": "Primary destination"
        },
        {
          "type": "select",
          "id": "category",
          "label": "Default shopping destination",
          "options": [
            {
              "value": "all",
              "label": "All parts"
            },
            {
              "value": "tires",
              "label": "Racing tires"
            },
            {
              "value": "brakes",
              "label": "Braking components"
            }
          ],
          "default": "all"
        },
        {
          "type": "text",
          "id": "secondary_label",
          "label": "Secondary link"
        },
        {
          "type": "url",
          "id": "secondary_link",
          "label": "Secondary destination"
        },
        {
          "type": "select",
          "id": "artwork",
          "label": "Included photography",
          "options": [
            {
              "value": "hero",
              "label": "Circuit"
            },
            {
              "value": "slide-tires",
              "label": "Tires"
            },
            {
              "value": "slide-brakes",
              "label": "Brakes"
            }
          ],
          "default": "hero"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "BXR cinematic slideshow",
      "blocks": [
        {
          "type": "slide"
        }
      ]
    }
  ]
}
{% endschema %}
